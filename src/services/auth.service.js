import { useFetch } from "../hooks/useFetch";

const baseUrl = "https://rickandmortyapi.com/api/";
const characterUrl = baseUrl + "character/";

const getUser = (data) => {
    // const { data, error, loading } = useFetch(characterUrl + "2");
    return "";
    // if (error) {
    //     return "";
    // }
    // if (!loading) {
    //     console.log(data)
    //     return data;
    // }
};

export default getUser;
